# tests/

Pytest unit and integration tests for the runtime application in `src/`.

Empty as of the SDLC foundation slice. Test files will be added alongside the runtime component they cover, per `docs/02_testing/TEST_STRATEGY.md`. Every implementation PR is expected to add or update tests here (see Definition of Done).
